# sales_analyzer.py

SalesAnalyzer – Python-скрипт для анализа CSV-файлов с продажами.
Что делает:
Генерирует CSV с данными о продажах (дата, товар, количество, цена)

Загружает и обрабатывает CSV (очищает пропуски, преобразует типы)

Считает выручку по каждому товару и общую выручку

Рассчитывает среднюю цену товаров

Сохраняет статистику в JSON

Визуализирует выручку по товарам (Matplotlib) с горизонтальной линией средней цены



💡💡  Я постарался почти на каждую строчку кода сделать комментарий, чтобы точно все было понятно. 💡💡
